import React from 'react';
import Typography from '@material-ui/core/Typography';

const Landing = () => {
    return (
        <div>
            <Typography variant="title">Emaily</Typography>
            <Typography variant="body1">Collect feed back from users</Typography>
        </div>
    );
}

export default Landing;